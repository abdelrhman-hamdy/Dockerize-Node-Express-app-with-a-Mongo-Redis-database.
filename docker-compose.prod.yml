version: '3'
services: 

  nginx:
    ports:
      - "80:80"

  node-app:
    build:
      context: .
      args:
        - NODE_ENV=production
#    ports:
#      - "3500:3500"
    environment:
      - PORT= 3000
      - NODE_ENV=production
    entrypoint: npm run start